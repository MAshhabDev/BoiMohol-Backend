model Author {
  id        String   @id @default(uuid())
  name      String
  slug      String   @unique
  bio       String?
  photoUrl  String?
  createdAt DateTime @default(now())
  books     Book[]
  @@map("authors")
}